<div class="edit-task">
  <%=form_for(task, url: task_path(task), method: :patch, remote: true ) do |f|%>
  <%=f.text_field :body%>
  <%=f.submit "Update", class: "btn btn-success inline"%>
  <div class="cancel-edit inline btn btn-danger">
    cancel
  </div>
  <%end%>
</div>
